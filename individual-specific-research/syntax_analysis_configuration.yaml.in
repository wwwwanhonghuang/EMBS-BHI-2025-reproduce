syntax_analysis:
  grammar_file: "${GRAMMAR_FILE}"
  input: "${INPUT_FILE}"
  repetition: "${REPETITION_FILE}"
  log_intervals: 1000000
  log_path: "${LOG_PATH}"
  report_path: "${REPORT_PATH}"
  serialize_to_files: true
  tree_serialization_path: "${TREE_STORE_PATH}"
  report_statistics: false
  sentence_from: -1
  sentence_to: -1
